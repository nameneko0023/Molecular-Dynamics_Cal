#時間をつなげる

import Path_pull as PP
import file_processer as file
def conecter(file0, file1, file2, file3, file4):

    try:
        base = file.csv_reader(file0)
        time1 = file.csv_reader(file1)


try:
    base = PP.csv()
    file1 = PP.csv()
    conecter(base, file1)

pass
